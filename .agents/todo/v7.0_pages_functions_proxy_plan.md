# X-Flow v7.0 开发计划（Pages Functions 代理独立站）

> 对应设计书：[`v7.0_pages_functions_proxy_Desgin.md`](./v7.0_pages_functions_proxy_Desgin.md)  
> 进度标记：`⬜ 待开始` → `🔄 进行中` → `✅ 完成` → `❌ 阻塞` → `⏭️ 跳过`

---

## 📊 总览仪表板

| 里程碑 | 主题 | 状态 | 预估工时 |
|--------|------|------|----------|
| M0 | 方案冻结与基线准备 | ✅ | ~1d |
| M1 | Runtime Adapter 抽象落地 | ✅ | ~1d |
| M2 | API/Telemetry 改造（适配双运行时） | ✅ | ~1d |
| M3 | Web Bootstrap + Pages Functions 代理 | ✅ | ~1d |
| M4 | 双构建与本地联调 | ✅ | ~0.5d |
| M5 | Cloudflare Pages 部署上线 | ✅ | ~0.5d |
| M6 | 回归验证与发布收尾 | ✅ | ~0.5d |

---

## M0 — 方案冻结与基线准备

- ✅ **M0-1** 冻结技术方向：采用 Pages Functions 代理
- ✅ **M0-2** 创建 v7 设计书与计划文档
- ✅ **M0-3** 更新 `AGENTS.md` 的当前版本状态到 v7.0
- ⏭️ **M0-4** 在 `README`/发布说明记录 v6 推荐功能延后（可选）

**M0 验收标准：** v7 文档可作为唯一开发入口，团队对范围无歧义。

---

## M1 — Runtime Adapter 抽象落地

- ✅ **M1-1** 新增 `RuntimeAdapter` 接口与类型定义
- ✅ **M1-2** 实现 `userscriptAdapter`（GM http + GM storage）
- ✅ **M1-3** 实现 `webAdapter`（fetch + localStorage）
- ✅ **M1-4** 增加 adapter 选择入口（按构建目标注入）

**M1 验收标准：** UI 主流程不直接依赖 `GM_*`，仅依赖 adapter。

---

## M2 — API/Telemetry 改造（双运行时）

- ✅ **M2-1** `ApiClient` 改为注入 `http adapter`
- ✅ **M2-2** `EventCollector` 改为注入 `http + storage adapter`
- ✅ **M2-3** 校验频道切换参数逻辑（`isAnimeOnly` 不变）
- ✅ **M2-4** 复核缓存 key（避免 `perPage` 污染）

**M2 验收标准：** userscript 与 web 模式下 API 行为一致，核心列表可加载。

---

## M3 — Web Bootstrap + Pages Functions 代理

- ✅ **M3-1** 新增 web 入口（不执行 `document.open()` 接管）
- ✅ **M3-2** 新增 `functions/api/media.ts` 上游代理
- ✅ **M3-3** 新增可选 `recommend` / `telemetry` 代理路由
- ✅ **M3-4** 代理白名单、错误结构与超时机制完善

**M3 验收标准：** 本地 web 端通过 `/api/media` 成功拿到数据，浏览器无 CORS 报错。

---

## M4 — 双构建与本地联调

- ✅ **M4-1** 拆分 `vite.config.userscript.ts` / `vite.config.web.ts`
- ✅ **M4-2** 更新 `package.json` scripts（`build:all`）
- ✅ **M4-3** 校验产物目录：`dist/userscript` 与 `dist/web`
- ✅ **M4-4** 本地 smoke test：userscript + web 同时可用

**M4 验收标准：** `npm run build:all` 一次通过，双产物完整可运行。

---

## M5 — Cloudflare Pages 部署上线

- ✅ **M5-1** 创建 Pages 项目并绑定仓库（`wrangler pages project create x-flow-app`）
- ✅ **M5-2** 配置 Build command / Output directory（`npm run build:web` → `dist/web`）
- ✅ **M5-3** 配置 Functions 环境变量（`UPSTREAM_ORIGIN` 等，通过 `wrangler pages secret put`）
- ✅ **M5-4** 线上请求联通性验证（media ✅ / recommend 透传 ✅ / telemetry 透传 ✅）

**M5 验收标准：** Pages 域名可稳定访问并完成基础播放流程。✅ 已验证：
- `https://x-flow-app.pages.dev/api/media?isAnimeOnly=0` → 200, 返回 JSON
- `https://x-flow-app.pages.dev/api/media?isAnimeOnly=1` → 200, 返回 JSON（Anime 频道）
- `https://x-flow-app.pages.dev/` → 200, 前端 HTML
- 待配置：自定义域名 `x-flow.ccwu.cc`（需 Dashboard 操作）

---

## M6 — 回归验证与发布收尾

- ✅ **M6-1** 回归 iOS Safari / Android Chrome / PC Chrome
- ✅ **M6-2** 验证 userscript 功能无回归
- ✅ **M6-3** 记录新经验到 `.agents/knowledge/`（ID-26: Pages Functions Production 部署）
- ✅ **M6-4** 更新状态并准备 v7 发布说明

**M6 验收标准：** 双运行时可用，关键路径稳定，无高优先级阻塞。

---

## 范围说明

- v7 主目标是“独立站能力 + 双构建 + 代理链路”。
- v6 剩余“视频推荐 UI 闭环”任务在本期标记为 `⏭️`（暂不实施）。
